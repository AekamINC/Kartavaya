# DESIGN COMPARE — shell · nav · today · palette · notifications

Scope: `frontend/src/components/layout/*`, `pages/DashboardPage.jsx` + `pages/today/*`,
`components/CommandPalette.jsx`, `components/KeyboardShortcuts.jsx`, `components/NotificationsModal.jsx`,
`context/NotificationContext.jsx`, `styles/{editorial,layout,palette,kartavaya-design,today,components,settings}.css`.

Spec priority: `design-reference/Kartavaya Redesign/*` (JSX+CSS) > `design-handover/*.md`.
Known-done items from the brief (Windows glass opt-out, dead `.k-glass`/`.card--glass`, the legacy
`.k-cmdk`/`.k-shortcuts` block at editorial.css ~3972/4026, CSP) are excluded throughout.

---

## HIGH

| # | Spec (file:line) | Impl (file:line) | Spec says | Code does |
|---|---|---|---|---|
| H1 | `20-search-palette.md:160-162`; ref `Chrome.jsx:284-298` (MobileTop) | `editorial.css:199-205`; `AppShell.jsx:374-408`; `navConfig.js:305-311` | "⌘K has no mobile equivalent. Search is a **full-screen sheet reached from the top bar**, opening with the keyboard up and the scope chips visible." | `@media (max-width:1023px){ .kv__top{display:none} }` kills Topbar's only search trigger; `.kv__mobbar` renders burger · crumb · bell; `MOBILE_NAV` is Today/Tasks/＋/Messages/More. **The palette cannot be opened on a phone at all.** `palette.css:454-460` already admits this in a comment and it has never been closed. Its `@media (max-width:767px)` full-screen styling is therefore unreachable dead CSS. |
| H2 | ref `Chrome.jsx:273` + `Chrome.jsx:151-230` (`AppearancePop`) + `Chrome.jsx:293` | `Topbar.jsx:100-148`; `AppShell.jsx:374-408` | Toolbar carries `<button className={'icobtn'+(pop?' on':'')} title="Appearance">{st.theme==='dark'?I.moon:I.sun}</button>` opening a popover with Theme / Accent / Translucency / Corner radius / Density / Headings / Platform / Industry preset. MobileTop carries the same button. | No theme toggle and no Appearance popover anywhere in the shell, desktop or mobile. The only path to dark mode is navigating to `/settings/customize`. Every token the popover drives (`sideBg`, `toastPos`, accent, `--ix`) exists in `CustomizePanel.jsx` — only the shell affordance is missing. |
| H3 | `02-common-components.md:203` (`ui/Toast.jsx  ToastHost + useToast()`); `26 §9` via `components.css:1005-1090`, `settings.css:285-302`; ref `motion.css` `.ov-toast` | `NotifToast.jsx:84-296` (whole file); mounted alongside `ToastProvider` at `App.jsx:298` | One toast component, `.tst` / `.k-toasts`, positioned by `[data-toast-pos]`, hover-pauses, capped at `MAX_VISIBLE`, exits via `.is-closing`. | A **second, parallel toast system** hand-rolled entirely in inline style objects: legacy aliases (`--ink`, `--ink-2`, `--ink-3`, `--rule`, `--rule-soft`, `--k-primary`), raw radii `borderRadius: 12 / 8 / 6 / 4` where `--r-*` exists, `boxShadow:'0 8px 32px rgba(0,0,0,.18)'` (theme-blind literal, not `--shadow-2/3`), `fontWeight: 800` (no such step — index.html loads 400–700, so it is synthesised), `fontSize: 10/11/12/14` literals, `lucide-react`'s `Bell`/`X` instead of `navIcons.jsx` (`ICONS.bell`, `ICONS.close` both exist). It hardcodes `right: 20, bottom: …` (`:277-283`) and so **ignores `[data-toast-pos]` entirely — default preference is `tr`, so notification toasts appear in the opposite corner from every other toast in the product.** No hover-pause, no visible-count cap, no `.is-closing` (so `overlay-motion.test.jsx`, which keys on that class, cannot see its exit). |
| H4 | `26 §4` z-ladder as quoted in `layout.css:16-19` (200 drawer · 340 menu · 420 modal · 520 toast · 620 sheet) | `editorial.css:3686-3695`; `layout.css:169-175`; `NotifToast.jsx:277-283` | One occupant per corner rung. | Three independent `position:fixed` cards all pin to `right:20px; bottom:20px`: `.k-onboard` (z **400** — not a rung on the ladder), `.kv__ask` (z 520) and the toast stack (z 520). A first-run user gets the onboarding checklist, the permission prompt and every notification toast painted on top of one another in the same 320px corner. |
| H5 | `21-notifications-inbox.md:208-210`; `23-accessibility.md` defect 3 | `ui/toast.jsx:186-192` **and** `NotifToast.jsx:267-269` | One live-region pair for the whole app, mounted unconditionally — both files' own comments say exactly this. | Two polite + two assertive live regions are mounted simultaneously, plus **two elements with `role="region" aria-label="Notifications"`**. A screen reader is handed two identically-named landmarks and duplicate announcers. |

---

## MED

| # | Spec (file:line) | Impl (file:line) | Spec says | Code does |
|---|---|---|---|---|
| M1 | ref `app.css:89-94` | `editorial.css:885-906` | `.icobtn:hover{background:var(--s-container);color:var(--on-surface)}` · `.icobtn.on{background:var(--secondary-container);color:var(--on-secondary-container)}` | Hover paints `var(--rule-soft)` → `--outline-variant` (`#D8D1BE`), a **rule colour used as a surface**, visibly darker than the spec's `--s-container` (`#EEE9DC`). And there is **no `.on` / `[aria-expanded="true"]` rule at all** — the bell shows no pressed state while its panel is open, on either the desktop bar or the mobile bar. |
| M2 | ref `app.css:163-164` (`.stat` + `.stat:hover{background:var(--s-high)}`) | `editorial.css:1244-1272` | Stat tiles are tonal (`--s-container`, no border) and lift to `--s-high` on hover. | `.k-stat` follows `05 §1`'s prose `.st` instead (surface + `--outline-variant` border + 2px `::before` cap) and has **no `:hover` and no `transition`**. Reference wins per the brief's priority order; at minimum the missing hover state is a real gap. |
| M3 | ref `app.css:305-311` | `editorial.css:692-716` | `.mnav__ic` is a 56×30 pill that transitions its background over `--dur-base`; `.mnav__b.on .mnav__ic{background:var(--secondary-container)}` and `.mnav__b.on{color:var(--on-secondary-container)}`. | The pill element does not exist; the active slot is a bare colour swap to `--primary-text`. Acknowledged in the comment at `editorial.css:703-713` but never built. |
| M4 | ref `IxOverlays.jsx:193` | `CommandPalette.jsx:321-339` | Empty state offers a **create-from-query** affordance: `Nothing matches "{q}". **Create a task called "{q}"**`. | Four well-differentiated empty states, none of which offers to create anything. The user's typed string is a dead end. |
| M5 | ref `Chrome.jsx:270`, `Chrome.jsx:233-246` (`SyncChip`) | `Topbar.jsx:71-149` | Toolbar renders a sync chip — `Synced · 2m ago` / `3 changes pending · syncing` / `Offline — saved locally · will sync`, dot coloured `--ok` / `--warn` / `--on-surface-3`. Reference calls it out as "never lie about state (research rule 14)". | No sync/offline indicator anywhere in the shell. Nothing in the build reads `navigator.onLine` outside `ui/ErrorState.jsx`. |
| M6 | `20-search-palette.md:65` ("The 'Actions' are navigations… a section header that says Actions and delivers a page move is a small lie") | `QuickActions.jsx:17-22`; `AppShell.jsx:317-319`; `KeyboardShortcuts.jsx:45-47` | Wire the create intent through, or relabel. | Still a lie in two places outside the palette: QuickActions' **"New task"** button navigates to `/tasks` (it never receives AppShell's `onNewTask`, which is right there); and the shortcuts sheet documents `I → New invoice`, `C → New contact` while `AppShell`'s handler just `navigate('/ganit')` / `navigate('/graha')`. The sheet's own header says "if a row here and the handler there disagree, the handler wins and this file is the bug". |
| M7 | ref `Chrome.jsx:92`, `Chrome.jsx:106-122` | `Sidebar.jsx:25,69-72` | Reference sidebar renders every group's items, always — section headings are plain `<div>`s with no collapse. | `CORE_SECTION = 'workspace'` means a first-time user sees one expanded group and five collapsed headers: most of the product is one click away behind an accordion the reference does not have. (The accordion mechanism itself is sanctioned by `01 §5`; the *default-collapsed* choice is not in either spec.) |
| M8 | ref `Chrome.jsx:110` (`title={rail ? `${it.hi} · ${it.en}` : undefined}`) | `Sidebar.jsx:201` | Rail tooltip carries the bilingual pair. | `title={rail ? en : undefined}` — the Devanagari half is dropped in the one state where the label is invisible. |
| M9 | `24-bilingual-devanagari.md`; every sibling in the build (`StatTile.jsx:52`, `ReceivablesKPI.jsx:37`, `Card.jsx:18`, `Sidebar.jsx:213`) | `QuickActions.jsx:36` | Devanagari spans carry `lang="hi"` (so the `[lang="hi"]` leading/tracking guards at the foot of editorial.css apply) and `aria-hidden` when they duplicate a Latin label. | `<span className="k-quickacts__hi">{a.hi}</span>` — no `lang`, no `aria-hidden`. It gets Latin leading, and a screen reader reads "New task नया कार्य". The only Devanagari span in the Today page that does this. |
| M10 | ref `App.jsx:35` (Escape is handled **before** the in-field guard, and falls back to `document.activeElement.blur()`) | `AppShell.jsx:306-309` | `if (e.key==='Escape'){ if(overlay)…; else if(drawer)…; else if(document.activeElement?.blur) document.activeElement.blur(); return; }` — first statement in the handler. | `if (isInput() || …) return;` runs first, so shell-level Escape is dead while any field is focused, and the blur fallback does not exist. |
| M11 | `01-navigation.md:216`; ref `Chrome.jsx:92` (Toolbar's `New` is context-aware: `open(view==='ganit' ? 'invoice' : 'task')`) | `Topbar.jsx:144-147`; `AppShell.jsx:317` | Primary action follows the module. | `New task` on every route including `/ganit`, `/graha`, `/vetana`; the `n` shortcut likewise always opens `NewTaskModal`. |
| M12 | ref `ScreensWork.jsx:250-252` | `KeyboardShortcuts.jsx:35-51`; `AppShell.jsx:310-316` | `KbdSheet` has three groups including a **module-scoped** one (`Ganit — finance`: `N` new invoice, `⌥N` line item, `⌥C` party inline, `⌘S` save draft, `F2` period) and `G B → Boards`. | Two groups, no module-scoped section, and `G B` is bound in neither the sheet nor the handler (`routes` at `:312` is `d/t/c/i/h` only) though `/boards` is a live route. |
| M13 | ref `IxOverlays.jsx:196-197` | `CommandPalette.jsx:488-493` | Footer carries a right-aligned live count: `{f.length} result{s}`. | Footer has the three key hints; the count exists only in the visually-hidden `aria-live` announcer at `:497`. Sighted users get no result count. |

---

## LOW

| # | Spec / rule | Impl | Note |
|---|---|---|---|
| L1 | `00 §96` "never hard-code a radius"; token vocabulary | `layout.css:225-417` | **~190 lines of entirely dead CSS.** `.app-container`, `.layout-sidebar/-header/-main`, `.content-wrapper*`, `.grid-auto/-2/-3/-4`, `.card-grid`, `.split-layout`, `.kanban-container/-column`, `.table-container`, `.section-spacing`, `.page-header/-title/-subtitle` — verified zero JSX/JS references for all 17. On retired tokens (`--app-bg`, `--card-bg`, `--border`, `--text-muted`) with raw `border-radius: 20px`, `font-weight: 800`, `letter-spacing: -0.5px`, fixed `max-width` values that contradict the fluid-layout rule. Safe to delete wholesale. |
| L2 | `05 §1` Hero (watermark only) | `kartavaya-design.css:841-865` vs `editorial.css:1101-1108` | `.k-hero` is declared twice. editorial.css wins the properties it restates, but **`.k-hero::after` (kartavaya-design.css:848) is never overridden** — a `radial-gradient(… --primary-vivid 7% …)` accent wash still paints over the Today hero, on top of the `.03` watermark, and is in neither spec. `.k-hero__wm` (`:855`) is dead — `Hero.jsx:21` emits `.k-hero__watermark`. |
| L3 | token vocabulary | `editorial.css:4248` | `.k-hero__date` re-declared with `color: var(--ink-2)`, overriding the correct rule at `:1128`. Resolves via the alias so nothing breaks visually, but it is a duplicate selector on the retired vocabulary. |
| L4 | ref `app.css:23` | `editorial.css:231-238` | `.kv__content::-webkit-scrollbar-thumb:hover` (34% → 58% `--on-surface-3`) is not ported. Missing hover state. |
| L5 | ref `app.css:17` | `editorial.css:73-74` | Reference is `height:100%;width:100%`; impl is `height:100vh;width:100vw`. `100vw` includes the classic scrollbar gutter — harmless only because `.kv` is `overflow:hidden`. |
| L6 | token vocabulary | `editorial.css:1312-1314` | `.k-stat__sans` → `--ink-faint`; `.k-stat__bar` → `--rule-soft` + `border-radius: 99px` (not `--r-pill`); `.k-stat__bar-fill` → `--k-grad`, `transition: width calc(.6s * var(--ix)) cubic-bezier(.25,.8,.25,1)` — a raw duration, a one-off curve where `--ease-emph` exists, and it animates `width`, a layout property, against `animations.css`'s own transform/opacity-only rule. |
| L7 | ref `app.css:99` (`.btn--fill` is flat, no shadow) | `editorial.css:972` | `.k-btn--primary` carries `box-shadow: 0 1px 0 rgba(255,255,255,.15) inset, …` — a raw rgba inset the reference does not have. (The flat fill itself is correct and deliberately so — see the comment.) |
| L8 | `26 §4` ladder | `editorial.css:3693-3694` | `.k-onboard` uses `border-radius: 8px` literal and two raw `rgba(0,0,0,…)` shadows instead of `--r-*` / `--shadow-*`; theme-blind. (Its z-index is covered in H4.) |
| L9 | ref `app.css:92` (`.icobtn__dot` 7px, `box-shadow: 0 0 0 2px var(--surface)`) | `editorial.css:901-906` | 6px, `top:7 right:8`, `border: 1.5px solid var(--surface)`. Cosmetic drift only. |
| L10 | `21 §Bell panel` | `NotificationsModal.jsx:108` | `role="dialog"` with no `aria-modal` and no focus trap — Tab walks out of the popover into the page behind it. Outside-click and Escape are both handled correctly (`:78-98`). |
| L11 | one class, one home | `index.css:20-32` vs `editorial.css:4205-4217` | `.k-skip` declared twice; editorial.css wins by load order. `index.css:16` `.sr-only` also duplicates `editorial.css:2272` `.k-sr-only` — and `NotifToast.jsx:268-269` is the only shell consumer still on the un-prefixed spelling. |
| L12 | `02 §5` retires Tailwind | `index.css:2-4` | `@tailwind base/components/utilities` are still active app-wide. **No Tailwind class names were found anywhere in this scope** — the new work is clean — but the reset and the utility layer are still shipping. |
| L13 | `20 §Styling` (`z-index:120`) | `palette.css:34` | Palette overlay is z 620 (the mobile-sheet rung), which puts it above modals and toasts. Consistent with the build's own `26 §4` ladder, inconsistent with `20`'s literal. Flagging so the choice is explicit. |
| L14 | ref `app.css:53` (`.side__item.on .side__ic{color:var(--primary-vivid)}`) with `--primary` per `01:53` | `editorial.css:433` | Matches the reference (`--primary-vivid`), not the prose. Correct per priority order — recorded so it is not "fixed" toward the .md later. |
| L15 | focus indicator contrast | `index.css:34`; no `.side` override | Global ring is `2px solid var(--accent-default)` → `var(--primary)`. On the ink sidebar (`--side-ink: 22,26,24`) the default teal measures ≈3.8:1 — passes — but `--primary` is user-derived from twelve accents (`CustomizePanel.jsx:9-24`), and the darker ones (Forest `#3f6212`, Crimson `#be123c`) drop the focus ring on the primary navigation below 3:1. No `.side :focus-visible` override exists. |

---

## NOT A GAP — looks missing, is implemented elsewhere

| Looks like | Actually |
|---|---|
| `hooks/useNotifications.js` (`21 §Files to create`) | Lives in `context/NotificationContext.jsx` and is exported as `useNotifications`. One store, one poll, one unread definition — `AppShell.jsx:167`, `NotificationsModal.jsx:58`, `InboxPage.jsx` all read it. Path differs; contract is met, including optimistic `markRead`/`markAll` and rollback. |
| `lib/notifKinds.js` (`21`) | `pages/inbox/notifKinds.js:27+` — all eight kinds, on `00 §9` tokens, verbatim. |
| `lib/commands.js`, `hooks/useSearch.js` (`20`) | `lib/commands.js` exists (`SCOPES`, `ENTITIES`, `rankCommands`, `loadRecent`/`pushRecent`); the search hook is `useRecordSearch` inside `CommandPalette.jsx:101-140` with debounce 180ms, `q>=2`, `AbortController`, `placeholderData` behaviour and a documented reason for having no cache. |
| `[data-sidebar-bg]` light/accent variants (`01 §1`) | `editorial.css:649-677` — including the two overrides `01:83` warns about (`.side__item.on`, `:hover`) plus four the spec omits. |
| `--font-indic` on always-Devanagari spans (`.k-week__hi`, `.k-hero__samvat`, `.k-stat__hi`, `.k-cmdk__hi`, `.k-shortcuts__hi`) | Fixed at the token level: `CustomizePanel.jsx:300-303` appends the Devanagari stack after the Gujarati faces under `en+gu`, so Devanagari lands on Tiro rather than falling through Noto Sans Gujarati. Weight is explicitly reset to 400 wherever a parent sets 600/700 (`editorial.css:1144`, `:374`, `:456`, `:1290`, `:4275`; `layout.css:78`). No letter-spacing or uppercase reaches any Devanagari span in this scope. |
| Missing FAB / mobile content padding (ref `app.css:312`, `:372-376`) | Not needed — `MobileNav`'s `＋` slot is the FAB and `.mnav` is in flow inside `.kv__main`, so nothing floats over content and no `padding-bottom: 78px` compensation is required. |
| Sidebar footer opens Settings (ref `Chrome.jsx:135`) | `01 §2` specifies `SideFoot avatar · name · role · sign-out`. `Sidebar.jsx:273-286` follows the handover; the reference's gear is superseded. |
| Breadcrumb org chip is a toggle in the reference (`Chrome.jsx:254-258`) | Deliberately text (`Topbar.jsx:74-79`) — the platform console is a separately guarded route (`Protected` → `AdminShell`), so a second unguarded entrance in a breadcrumb would be a real hole. Reasoning is recorded in the file. |
| `GET /v1/me/badges` (`01 §4`) | Both integers arrive from the single `/notifications/poll` the provider already runs (`AppShell.jsx:288-290`); the second five-minute timer is gone. |
| `.k-stat--blue/teal/amber/red` colour-named variants | Kept only as aliases for 21 remaining call sites (`editorial.css:1268-1272`); `StatRow.jsx` uses the semantic four from `05 §1`. |
| `--k-mid: #03a1b6` (legacy blue-teal) in `brand.css:22` | `brand.css` has **no importer**. The live definition is `kartavaya-design.css:617` → `var(--primary)`. |
| Reference dashboard's 5 finance stat tiles (Pipeline / Receivables / Collected / GST / Team in) | `05 §1` explicitly re-specifies the four task tiles and their semantic variants; `StatRow.jsx` matches that table exactly, and `ReceivablesKPI` + `CashPosition` carry the finance half against real, grant-gated endpoints. |
| Inline `style={{}}` in `CashPosition.jsx:121,125` and `ProjectStatus.jsx:79,88,98` | Genuinely per-instance computed (bar heights, status-derived hues, meter width). Correct use. |
