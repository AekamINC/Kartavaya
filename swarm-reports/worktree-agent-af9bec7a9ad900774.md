# Landing page · Search & command palette · Global motion

Agent branch: `worktree-agent-af9bec7a9ad900774`
Base: `origin/staging` @ 666b0ea
Status: **IN PROGRESS** — this file is written as work proceeds, per the swarm rule.

> Worktree note: this worktree was created from `main`, not `staging` — it started
> 13 commits ahead on production history with no `design-handover/` at all.
> `git reset --hard origin/staging` before any work. Any other agent seeing a
> missing `design-handover/` has the same problem.

---

## 1. Landing page

### Largest phrase on the page — the script bug

Pending measurement.

## 2. Search and the command palette

Pending.

## 3. Global motion system

Pending.

## Stale claims

Pending.
