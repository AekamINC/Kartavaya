# Avatar

Circular initials avatar. Auto-picks a brand color from the name hash. `ring` adds a surface-colored outline for stacking. `me` applies the brand gradient.

```jsx
<Avatar name="Keval Shah" size={28} />
<Avatar name="Aanya Mehta" size={28} ring />
<Avatar name="Keval Shah" me size={32} />
```
