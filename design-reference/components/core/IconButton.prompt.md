# IconButton

Square 34×34 icon button used in the topbar for notifications and settings. Optional red dot for unread state.

```jsx
<IconButton title="Notifications" dot={unread > 0}>
  <BellIcon />
</IconButton>
```
