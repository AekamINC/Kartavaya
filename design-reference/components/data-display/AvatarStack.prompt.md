# AvatarStack
Overlapping circular initials. Auto-picks brand colors. Shows up to `max` avatars, then a +N badge.

```jsx
<AvatarStack users={[{ name: 'Keval Shah' }, { name: 'Aanya Mehta' }]} max={3} size={22} />
```
