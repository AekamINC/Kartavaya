# StatusChip
Pill chip with colored dot and label. Maps status keys (todo, in_progress, in_review, done, requested, pending, approved, rejected) to brand colors.

```jsx
<StatusChip status="in_progress" />
<StatusChip status="done" />
<StatusChip columnName="To Do" columnColor="#94a3b8" />
```
