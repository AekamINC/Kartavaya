/**
 * Moved to `ui/StatusChip.jsx` (02-common-components.md §5). The precedence it
 * encodes is correct and unchanged: an active or decided approval state wins
 * over the column, and the column wins over the raw status field.
 */
export { default } from '../ui/StatusChip';
