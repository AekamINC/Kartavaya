# Pick up here — paused 17 Aug 2026, evening

Two commits landed on `staging` after this was first written, both unpushed:
`381d6324` (proposals 60-63) and `88ffabf7` (Dristi phase D1, the date window).
Everything else below is the agreed plan and the research already done, so the
next session starts at the keyboard.

## The order

1. **Niyam system account** — decided by the owner 17 Aug, never built.
2. **A4 · the invoice reminder ladder** — the one approved automation. Needs 1.
3. **Low stock + Pahchan attendance reports get a sender** — both collect
   recipients and produce nothing. Emit as Niyam events, not two bespoke senders.
4. **Mobile pull-to-refresh** — 13 consumers, stripped to dodge the RN 0.81
   list-blanking bug. Diagnose it; cold-restart every probe, hot reload lies.
5. **The whole Dristi plan**, in the proposals' own order:
   D2 registry (~2) → D3 saved views (~2) → D3b branded exports + glass +
   module tabs (~1) → D4 Ganit/core (~2) → D5 the other eleven (~3) →
   D6 presets (~1) → D7 alerts into Niyam (~1-2), then proposal 60's
   A1 spine → A2 Meta as adapter → A3 /cron/analytics unarmed → A4 GA4 →
   A5 the blended report → A6 export.
   **D1 is already shipped** (`88ffabf7`). Total remaining ~20 sessions.

## What the system account needs (research done, no code written)

- `public.users` is the table (unqualified — public schema). `user_id` is TEXT,
  and `password_hash` / `salt` / `email` / `name` are all NOT NULL.
- Shape: one row per org, `user_id = 'niyam:<org_id>'`, name `Niyam`, an
  unroutable email, and a password hash no input can produce. Add
  `is_system BOOLEAN NOT NULL DEFAULT FALSE` and refuse it at the login path
  explicitly rather than trusting the sentinel hash alone.
- **Seats:** `org_invites.count_seats` counts DISTINCT `user_roles.user_id`
  holding a `SEAT_ROLES` code, so the account must NOT hold one of those codes.
  `SEAT_ROLES` is built from `middleware/role_tiers.SEAT_CONSUMING_ORG_ROLES`;
  `user_roles.role_code` has a CHECK list in migration 016 that a new `system`
  code has to be added to.
- **Where it must not appear:** member lists, @mention pickers, team counts.
- Why it matters beyond tidiness: `services/niyam/actions.py` says outright that
  `task.add_comment` is missing because the comment read path INNER JOINs
  `users`, so a comment from a non-existent author is invisible to everyone.
  This account is what unblocks that verb.
- Next migration number is **148** (147 is `suppressed_is_a_status`).

## Still owed by the owner

Both under 15 minutes, both sitting since 16 Aug — see
`docs/OWNER-CHECKLIST-2026-08-16.md`:

1. Rotate the reminder-cron dispatch secret (it is a URL query string today).
2. Arm `scraper-prices` on `cron-daily` — **and redeploy**, because a config
   edit is not a deployment.
