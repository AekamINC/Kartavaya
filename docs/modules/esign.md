# E-Sign प्रमाण — Electronic signatures

**Module code** `esign` · registered in `backend/middleware/role_tiers.py`

In-house signing — deliberately not Aadhaar. A document is prepared, sent to a signer, and returns an audit trail: who signed, from where, and when.

## Flow

A signature request stores the document in R2 and issues a tokenised link. The signer opens it without an account, signs, and the signed artefact plus its trail are written back against the originating record.

## Backend

- `backend/routers/esign.py`

**Services**
- `backend/services/esign_service.py`

**13 routes** — 4 GET, 9 POST

<details><summary>All routes</summary>

- `GET /documents`
- `POST /documents`
- `POST /documents/{doc_id}/upload`
- `GET /documents/{doc_id}`
- `POST /documents/{doc_id}/send`
- `GET /verify/{token}`
- `POST /verify/{token}/otp/send`
- `POST /verify/{token}/otp/verify`
- `POST /verify/{token}/sign`
- `POST /verify/{token}/decline`
- `POST /documents/{doc_id}/cancel`
- `POST /documents/{doc_id}/resend/{signer_id}`
- `GET /documents/{doc_id}/audit`

</details>

## Database

3 tables:

- `sign_audit_log`
- `sign_documents`
- `sign_signers`

## Frontend

- `frontend\src\pages\esign\CreateTab.jsx`
- `frontend\src\pages\esign\DetailTab.jsx`
- `frontend\src\pages\esign\DocumentsTab.jsx`
- `frontend\src\pages\EsignPage.jsx`
- `frontend\src\pages\ganit\ESignTab.jsx`

**Components**
- `frontend\src\components\documents\EsignStatusPill.jsx`

## Integrations

- AWS SES
- Cloudflare R2

---
_Routes, tables and paths are generated by `scripts/module-facts.mjs` and
`scripts/gen-module-docs.mjs`. Re-run both after changing the module; do not
edit those sections by hand. Purpose and Flow are hand-written._
