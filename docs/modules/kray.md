# Kray क्रय — Procurement

**Module code** `kray` · registered in `backend/middleware/role_tiers.py`

Purchase orders, receipts and vendor bills — the money going out, mirroring Vikray. It owns no tables of its own: vendors, orders and bills all live in Ganit (`ganit_vendors`, `ganit_purchase_orders`, `ganit_vendor_bills`), and Kray is the discipline over them.

## Flow

An order is drafted against a vendor and approved against thresholds the org sets; amending it past them requires approving again. Issuing assigns a PO number and stamps `issued_at` — a state change, not a transmission: no PO document is rendered or sent. Receipts record what arrived, and the three-way match compares order, receipt and bill, flagging discrepancies and approving nothing.

## Backend

- `backend/routers/procurement.py`


**22 routes** — 11 GET, 1 PUT, 8 POST, 1 PATCH, 1 DELETE

<details><summary>All routes</summary>

- `GET /settings`
- `PUT /settings`
- `GET /approver-candidates`
- `GET /purchase-orders`
- `POST /purchase-orders`
- `GET /purchase-orders/{po_id}`
- `PATCH /purchase-orders/{po_id}`
- `DELETE /purchase-orders/{po_id}`
- `POST /purchase-orders/{po_id}/submit`
- `POST /purchase-orders/{po_id}/issue`
- `GET /approvals`
- `POST /purchase-orders/{po_id}/approve`
- `POST /purchase-orders/{po_id}/reject`
- `POST /purchase-orders/{po_id}/receipts`
- `POST /purchase-orders/{po_id}/close`
- `GET /purchase-orders/{po_id}/match`
- `POST /vendor-bills/{bill_id}/link`
- `GET /reports/committed-spend`
- `GET /reports/received-not-invoiced`
- `GET /reports/late-suppliers`
- `GET /reports/tds-194q`
- `GET /reports/budget`

</details>

## Database

12 tables:

- `ganit_po_approvals`
- `ganit_po_lines`
- `ganit_po_receipts`
- `ganit_po_revisions`
- `ganit_products`
- `ganit_purchase_orders`
- `ganit_vendor_bills`
- `ganit_vendors`
- `organisations`
- `statute_calendar`
- `user_roles`
- `users`

## Frontend

- `frontend\src\pages\kray\BudgetsTab.jsx`
- `frontend\src\pages\kray\KrayReportsTab.jsx`
- `frontend\src\pages\kray\KraySettingsTab.jsx`
- `frontend\src\pages\kray\VendorsTab.jsx`
- `frontend\src\pages\KrayPage.jsx`


## Integrations

- AWS SES

---
_Routes, tables and paths are generated by `scripts/module-facts.mjs` and
`scripts/gen-module-docs.mjs`. Re-run both after changing the module; do not
edit those sections by hand. Purpose and Flow are hand-written._
